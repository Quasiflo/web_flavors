---
name: web_flavors
description: Flavor support for Flutter web. A tiny wrapper CLI that fabricates web/ from web-flavors/common plus a per-flavor overlay, then forwards to flutter.
version: 0.2.0
homepage: https://github.com/Quasiflo/web_flavors
repository: https://github.com/Quasiflo/web_flavors
issue_tracker: https://github.com/Quasiflo/web_flavors/issues
topics:
  - flutter
  - web
  - flavors

environment:
  sdk: ">=3.4.0 <4.0.0"

executables:
  web_flavors:

dependencies:
  args: ^2.7.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  dependency_sorter: ^0.2.0
  quasiflo_lints: 0.1.1
  test: ^1.25.0
