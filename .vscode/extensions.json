{
    // Recommended extensions for working in this repo (VS Code prompts to install these).
    // Each one maps to repo tooling: Dart/Flutter (package + example apps),
    // rumdl (markdown formatter/linter), mise (pinned toolchain), Pkl (hk hooks),
    // YAML (pubspec/workflows), TOML (mise config), GitHub Actions (CI workflows).
    "recommendations": [
        "dart-code.dart-code",
        "dart-code.flutter",
        "rvben.rumdl",
        "hverlin.mise-vscode",
        "pkl.pkl-vscode",
        "redhat.vscode-yaml",
        "tombi-toml.tombi",
        "github.vscode-github-actions"
    ]
}
